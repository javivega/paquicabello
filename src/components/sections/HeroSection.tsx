/**
 * Legacy entry — the marketing hero lives in `HomeHero`.
 * Re-export keeps imports stable and avoids a second, out-of-sync layout.
 */
export { HomeHero as HeroSection } from '@/components/home/HomeHero'
